# local environments
.venv/
.env

# Python caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Datasets: generated/downloaded locally, do not commit
/data/raw/
/data/processed/
/data/cache/
/data/raw/
# Large raw result dumps. Keep curated CSV summaries/manifests only if needed.
/results/raw/
/results/checkpoints/
/results/models/
*.pt
*.pth
*.npz
.DS_Store
**/.DS_Store
paper-ideas/CoalGameRec/code/.DS_Store
# NOTE: the blanket "**.gz" rule was removed in round 6 — the released per-user
# inference artifacts (per_user_metrics*.csv.gz) are REQUIRED for the paper's
# statistical tables and must be committed.
next-paper/**/.DS_Store
