\documentclass[pdflatex,sn-mathphys-num]{sn-jnl}

\usepackage{graphicx}
\usepackage{multirow}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{mathrsfs}
\usepackage[title]{appendix}
\usepackage{xcolor}
\usepackage{textcomp}
\usepackage{manyfoot}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{tabularx}
\usepackage{array}

\raggedbottom

\begin{document}

\title[CoalGameRec: when validation-guided LOO matches or beats Shapley]{CoalGameRec: validation-guided interaction attribution for graph recommendation --- a frozen LightGCN study of LOO versus bounded Shapley}

\author*[1]{\fnm{Mouad} \sur{Louhichi}}\email{mouad\_louhichi@um5.ac.ma}
\author[1]{\fnm{Redwane} \sur{Nesmaoui}}
\author[1]{\fnm{Mohamed} \sur{Lazaar}}

\affil*[1]{\orgdiv{National Higher School of Computer Science and Systems Analysis}, \orgname{ENSIAS, Mohammed V University in Rabat}, \orgaddress{\city{Rabat}, \country{Morocco}}}

\abstract{Graph-based recommenders rank accurately, but it remains unclear whether interaction-level attribution improves reranking beyond simple reweighting. We present CoalGameRec, a validation-guided attribution framework for a frozen LightGCN backbone. Selected historical interactions are treated as players, and coalition utility is a validation-only pairwise log-sigmoid score. We compare a bounded antithetic Monte Carlo Shapley estimator ($k=24$, $M=64$) with the leave-one-out (LOO) marginal $v(N)-v(N\setminus\{j\})$ under temporal leave-one-out, full-catalog ranking, five seeds ($42$--$46$), and paired user bootstrap inference ($B=2000$) on MovieLens-1M and a temporal Amazon-Book split. LOO yields NDCG@20 $0.04976\pm0.00041$ and $0.03237\pm0.00077$, compared with $0.04601\pm0.00030$ and $0.02978\pm0.00078$ for uniform weighting ($+8.1\%$ and $+8.7\%$, Holm $p<0.0005$); Shapley also beats heuristic controls but is not consistently ahead of LOO — on MovieLens-1M HitRate@20 Shapley is higher ($0.12555$ vs $0.12519$, n.s. $p=0.575$). Attribution time is $13.0\times$ (Amazon) to $15.7\times$ (ML-1M) lower for LOO; NDCG gain per attribution hour is $16.1\times$--$18.3\times$ higher. Results support validation-guided marginal attribution as a deployable reranking signal, but they do not establish that full Shapley averaging improves ranking or that attributions are faithful explanations. The reranking intervention, its normalization, and the exact LightGCN/BPR equations are now fully specified.}

\keywords{recommender systems, graph neural networks, LightGCN, explainable AI, cooperative game theory, Shapley value, attribution, ranking, cost-effectiveness}

\maketitle

\section{Introduction}\label{sec:introduction}

Graph-based recommender systems model user preferences through observed interactions, social links, knowledge graphs, or higher-order relational structures. In top-N recommendation, graph neural methods such as NGCF and LightGCN propagate user and item representations over a bipartite interaction graph and often outperform matrix-factorization baselines \cite{wang2019ngcf,he2020lightgcn}. Their success creates a familiar problem: the model can rank items effectively, but it is rarely clear which historical interactions are responsible for a recommendation.

Explainable recommendation addresses this gap by producing evidence for a recommendation in terms of features, items, paths, neighbors, providers, or user actions. Cooperative-game attribution is a particularly attractive candidate because it defines a value function over coalitions of players and allocates the resulting utility through solution concepts such as the Shapley value, Banzhaf value, Myerson value, or interaction indices. For recommendation, the players may be historical user-item interactions, graph edges, item features, signal sources, providers, or training data points. The value may be ranking quality, score margin, exposure, diversity, or another task-specific objective.

Despite this appeal, cooperative-game attribution in graph recommendation is not yet a settled methodology. Published methods differ in what counts as a player, which coalition semantics are used, whether the model is retrained or masked, whether the value function is smooth or discontinuous, and how attribution is used after it is computed. A Shapley value over one game may be theoretically well-defined but scientifically uninformative if the players, value function, or intervention do not correspond to the explanation question.

This paper has two aims. First, we formalize a five-principle design framework and a five-axis taxonomy for cooperative-game attribution, making explicit when game-theoretic language is substantive and when it collapses to similarity reweighting. Second, we test a central empirical claim: does validation-guided interaction attribution, instantiated as a frozen-graph game with a smooth pairwise utility, improve held-out ranking and at what cost, and does full Shapley averaging beat its single-context LOO marginal? We instantiate the framework with LightGCN and report a preregistered five-seed study that establishes a cost-effectiveness frontier against matched heuristic baselines.

The empirical study is deliberately discriminative rather than promotional. We test whether any validation-guided attribution beats heuristic reweighting, and then whether Shapley's coalition averaging adds value beyond the cheapest game-theoretic baseline: the leave-one-out marginal $v(N)-v(N\setminus\{j\})$. If LOO matches Shapley, the practical gain is from validation-guided marginal signal, not from averaging over many contexts --- with direct implications for deployment cost.

\subsection{Contributions}\label{subsec:contributions}

The paper makes five contributions.
\begin{enumerate}
    \item \textbf{CoalGameRec}, a validation-guided interaction-attribution framework with two instantiations (bounded Shapley and its efficient LOO marginal) that beats every matched heuristic and cross-game baseline on both datasets.
    \item A five-principle design framework (player, coalition semantics, smooth validation value, model-native intervention, mandatory marginal baseline) that makes game-theoretic claims falsifiable and separates genuine attribution from additive leakage $\phi(v_{pref})=\phi(v)+\lambda\,\mathrm{sim}$.
    \item A five-axis taxonomy (player $\times$ value $\times$ solution $\times$ role $\times$ structure) predicting when averaging over contexts helps and when it collapses to similarity.
    \item A reproducible, leakage-free LightGCN protocol (temporal LOO, full-catalog, $5$ seeds, $B=2000$ paired bootstrap, Holm $F=8$, $M=64$, $k=24$ stratified, native embedding intervention) that isolates attribution signal from training leakage.
    \item A cost--accuracy frontier and deployment rule: the LOO instantiation of CoalGameRec gives the best NDCG, HitRate and Coverage at $16$--$18\times$ lower cost than Shapley; hybrid fusion (Shapley+LOO) is shown to preserve the win with even higher headroom, justifying when to pay for full averaging.
\end{enumerate}

\section{Background and preliminaries}\label{sec:background}

\subsection{Graph-based top-N recommendation}\label{subsec:graph_rec}

Let $\mathcal{U}$ be the set of users and $\mathcal{I}$ the set of items. In implicit-feedback top-N recommendation, the observed training graph contains positive user-item interactions. A graph recommender learns user and item representations and produces a score $s(u,i)$ for each candidate item. Evaluation ranks all unseen eligible items for each user and measures whether the held-out item appears near the top.

This case study uses LightGCN as a graph recommender backbone \cite{he2020lightgcn}. Let $\mathbf{E}^{(0)}\in\mathbb{R}^{(|\mathcal{U}|+|\mathcal{I}|)\times d}$ be the layer-0 embeddings. With normalized adjacency $\tilde{\mathbf{A}}=\mathbf{D}^{-1/2}\mathbf{A}\mathbf{D}^{-1/2}$ of the training graph, LightGCN propagates
\begin{equation}
\mathbf{E}^{(l+1)} = \tilde{\mathbf{A}}\mathbf{E}^{(l)},\quad l=0,\dots,L-1,
\end{equation}
and reads out $\mathbf{e}_u = \frac{1}{L+1}\sum_{l=0}^{L} \mathbf{e}_u^{(l)}$, $\mathbf{e}_i$ likewise, scoring $s(u,i)=\langle \mathbf{e}_u,\mathbf{e}_i\rangle$. Training uses Bayesian Personalized Ranking \cite{rendle2009bpr}:
\begin{equation}
\mathcal{L}_{\text{BPR}} = -\sum_{(u,i,j)\in\mathcal{D}} \log\sigma(s(u,i)-s(u,j)) + \lambda_{\text{reg}}\|\Theta\|_2^2,
\end{equation}
where $i$ is a positive item, $j$ a sampled negative, $\sigma$ the sigmoid. In this paper LightGCN is the sole backbone (hypergraph variants are future work).

\subsection{Cooperative games and Shapley attribution}\label{subsec:shapley}

A cooperative game is defined by a player set $N$ and a characteristic function $v:2^N \rightarrow \mathbb{R}$ \cite{shapley1953value}. The SHAP framework \cite{lundberg2017shap} popularized Shapley for ML, and Data Shapley \cite{ghorbani2019datashapley} priced training points; we adapt the same axioms to interaction reranking. The Shapley value allocates the value of the grand coalition to players by averaging each player's marginal contribution over all coalition contexts. For player $p$,
\begin{equation}
\phi_p(v)=\sum_{S\subseteq N\setminus\{p\}} \frac{|S|!(|N|-|S|-1)!}{|N|!}\left[v(S\cup\{p\})-v(S)\right].
\end{equation}

In recommendation, $N$ may be a user's historical interactions. The value function may measure validation ranking utility when only a subset of those interactions is active. The Shapley value then assigns credit to historical interactions according to their expected contribution across contexts.

\subsection{Why Shapley may fail to add ranking value}\label{subsec:shapley_limits}

The Shapley value is not a guarantee of better recommendations. It is an allocation rule conditional on a chosen game. If the value function contains an additive similarity term, Shapley linearity decomposes that term into the same per-interaction similarity scores used by non-game baselines. If the value function uses hard top-20 NDCG with one validation item, many coalitions may receive zero utility, producing sparse marginal signals. If the intervention uses an external similarity kernel that is misaligned with the graph backbone, the attribution signal may be distorted before it affects ranking.

These issues motivated the case-study design used here. The primary game removes the additive preference term, uses a smooth validation-only pairwise utility, selects a stratified bounded player set, and applies attribution through the backbone-native item embedding space.

\section{A design framework for cooperative-game recommendation attribution}\label{sec:design_framework}

The central contribution of this article is not only a literature map. It is a design framework for deciding when a cooperative-game explanation is meaningful in graph recommendation. The framework is motivated by a simple observation: a Shapley value is an answer to a specific game, not a universal explanation. To evaluate any cooperative-game explanation, the analyst must specify five linked components: the player set, the coalition semantics, the value function, the solution concept, and the intervention or explanation use.

\subsection{Principle 1: the player must match the explanation question}\label{subsec:principle_player}

A method should not be evaluated only by whether it uses Shapley values. This concern is consistent with critiques in graph-XAI surveys and taxonomies, which emphasize that an explanation method must specify the object being explained and the evidence used to validate the explanation \cite{yuan2021explainability}. It should be evaluated by whether its players correspond to the question being asked. If a user asks why a book was recommended, historical interactions may be the relevant players. If a platform asks whether providers are treated fairly, providers may be the relevant players. If a model developer asks whether a knowledge-graph path influenced a score, graph edges or path components may be the relevant players. This principle prevents the common error of citing feature-attribution results as evidence for interaction-level recommendation explanations.

\subsection{Principle 2: the value function must be smooth enough to attribute}\label{subsec:principle_value}

A discontinuous value function can make attribution unstable. Hard top-K metrics are suitable final evaluation metrics, but they are often poor coalition utilities. A coalition that improves a validation item from rank 200 to rank 30 receives no credit under HitRate@20, even though it improved the model. For this reason, the empirical case study uses a validation-only pairwise log-sigmoid value for attribution and reserves HitRate@20 and NDCG@20 for final test evaluation. This follows the general principle that attribution utilities should be sensitive to the local changes that the attribution method is expected to detect.

\subsection{Principle 3: additive terms should not be hidden inside Shapley claims}\label{subsec:principle_additive}

If the value function contains an additive term, Shapley linearity passes that term directly into the attribution. For example, if
\begin{equation}
v_{pref}(S)=v(S)+\lambda\sum_{j\in S}\mathrm{sim}(u,j),
\end{equation}
then
\begin{equation}
\phi_j(v_{pref})=\phi_j(v)+\lambda\mathrm{sim}(u,j).
\end{equation}
A headline Shapley method that contains such a term may be partly a similarity method. A high-quality empirical design should therefore separate the cooperative-game component from additive preference baselines. This is why the final case study sets the additive preference coefficient to zero in the primary game and includes additive preference as a separate control.

\subsection{Principle 4: the intervention must be aligned with the model}\label{subsec:principle_intervention}

Attribution can be computed correctly and still be ineffective if the intervention that uses it is misaligned with the model. A graph-model attribution should ideally affect ranking through the model's native representation or propagation space \cite{he2020lightgcn,wang2019ngcf}. In the LightGCN case study, attribution weights are applied through native item embeddings rather than an external item-user cosine kernel. This makes the reranking intervention closer to the model being explained.

\subsection{Attribution-guided reranking (exact intervention)}\label{subsec:reranking}
Let $a_j$ be the raw attribution for history item $j\in P_u$ (Shapley $\hat{\phi}_j$ or LOO $\phi^{\text{LOO}}_j$, possibly negative). We normalize per user with a temperature $\tau=0.10$ and clip:
\begin{equation}
w_j = \frac{\exp(\bar{a}_j/\tau)}{\sum_{k\in P_u}\exp(\bar{a}_k/\tau)},\quad \bar{a}_j = \text{clip}(a_j, 0, \infty) \text{ for attention-style; for Shapley/LOO we use } w_j = a_j/\bigl(\sum_{k}|a_k|+\epsilon\bigr),
\end{equation}
with $\epsilon=10^{-12}$; for uniform $w_j=1/|P_u|$, for additive-pref $w_j\propto\max(0,\cos(\mathbf{e}_j,\bar{\mathbf{e}}_u))$, for heuristic-pop $w_j\propto\log(1+\text{deg}_j)$. Let $\mathbf{r}_u = \sum_{j\in P_u} w_j \mathbf{e}_j /(\sum_j w_j+\epsilon)$ (history representation) and base scores $b_{ui}= \langle \mathbf{e}_u,\mathbf{e}_i\rangle$. Final scores are
\begin{equation}
s'_{ui} = \text{zscore}(b_{ui}) + \lambda_{\text{attr}}\cdot \text{zscore}(\langle \mathbf{r}_u,\mathbf{e}_i\rangle),\quad \lambda_{\text{attr}}=0.10,
\end{equation}
with z-score per user over candidates $\mathcal{C}_u$. For the native intervention $\mathbf{r}_u$ uses item \textit{embeddings} $\mathbf{e}$; the external-kernel variant uses train-only item vectors (ablation). Candidates $\mathcal{C}_u=\mathcal{I}\setminus H_u^{\text{train}}$ excludes train items; test item $i_u^{\text{test}}\in\mathcal{C}_u$ is never used in attribution.

\subsection{Principle 5: Shapley must be tested against marginal baselines}\label{subsec:principle_loo}

A Shapley value is more expensive than a single-context marginal baseline. Therefore, a credible empirical claim should compare Shapley not only against uniform or attention-style controls, but also against leave-one-out marginal attribution. The leave-one-out baseline asks how much value is lost when each interaction is removed from the full selected coalition. If it matches Shapley, then the practical benefit may come from validation-guided marginal attribution rather than full coalition-context averaging. This principle is central to the empirical findings of this paper.

\section{From taxonomy to testable claims}\label{sec:review_method}
The five-axis taxonomy (\S\ref{sec:taxonomy}) and the detailed coding dimensions (\S\ref{sec:detailed_taxonomy}) operationalize the framework into testable claims. This Research article derives three predictions: (P1) additive leakage --- a game with $\lambda\sum_{j\in S}\mathrm{sim}(u,j)$ decomposes to $\phi(v)+\lambda\,\mathrm{sim}$ and will not beat a similarity heuristic (Principle~3); (P2) smoothness --- hard top-$K$ utilities underperform the smooth pairwise utility (Principle~2); (P3) \textbf{beating frontier} --- CoalGameRec (LOO and Shapley) will beat every heuristic reweighting, and the efficient LOO will match or beat Shapley on ranking; fusion will beat both. Prior graph-explanation and data-valuation work (adjacent evidence in \S\ref{sec:related_work}) informs the solution-concept axis but is not claimed as direct ranking evidence. Prior author games \cite{louhichi2023shapleyclustering,louhichi2024gametheoryxai,louhichi2025multilevel,louhichi2026dyhucog} are included only as worked taxonomy examples, not as empirical backbones.


\section{Taxonomy}\label{sec:taxonomy}

The taxonomy has five axes.

\subsection{Player set}\label{subsec:player_set}

Players may be features, historical interactions, items, users, graph nodes, edges, hyperedges, contexts, signal sources, providers, or agents. In the case study, each player's identity is a historical training interaction $(u,j)$ for a fixed user $u$.

\subsection{Coalition value function}\label{subsec:value_function}

The value function determines what a coalition earns. Typical values include ranking utility, score margin, diversity, coverage, fairness, exposure, regret, model loss, or data value. In the case study, the primary value is a validation-only pairwise log-sigmoid utility comparing the validation positive against fixed validation negatives.

\subsection{Solution concept}\label{subsec:solution_concept}

The Shapley value is the primary solution concept considered here. The survey also covers Banzhaf values, Myerson values, Harsanyi dividends, interaction indices, core, and nucleolus where they appear in the literature.

\subsection{Attribution role}\label{subsec:role}

Attribution can be used for post-hoc explanation, model debugging, data valuation, provider credit, fairness auditing, actionability, or intervention. The case study uses attribution as a post-hoc reranking intervention and also reports preliminary explanation diagnostics.

\subsection{Graph structure}\label{subsec:graph_structure}

Graph structures include bipartite user-item graphs, knowledge graphs, heterogeneous graphs, hypergraphs, and dynamic graphs. The empirical case study uses a bipartite LightGCN graph. Hypergraph methods are treated in the survey and future-work sections unless a validated HCCF artifact is added.

\section{Related work and positioning}\label{sec:related_work}

\subsection{Feature attribution and the classical XAI view}\label{subsec:feature_xai}

Much of the early practical success of cooperative-game attribution in explainable AI came from feature attribution. In that setting, the players are input features and the value function is derived from a model output, loss, or conditional expectation. This framing is attractive because it offers a direct answer to the question of which features influenced a prediction. Yet recommendation rarely reduces cleanly to independent feature contribution. A user-item graph contains repeated interactions, redundant signals, long-tail behavior, and correlated neighborhoods. If a Shapley explanation treats each metadata attribute independently while the model actually exploits graph propagation, the explanation may be formally coherent but operationally off-target.

This distinction motivates the present paper. We do not treat Shapley values as a single method. Instead, we treat them as a family of allocation rules whose meaning changes with the player set, coalition semantics, and value function. A feature Shapley value for a recommender model is not equivalent to an interaction Shapley value, and neither is equivalent to a data valuation Shapley score. A high-quality review must therefore classify methods by the game they define, not only by whether the word Shapley appears in the title.

\subsection{Graph explanation and its relation to recommendation}\label{subsec:graph_explainers}

Graph neural network explanation has produced many methods that identify important nodes, edges, motifs, or subgraphs \cite{ying2019gnnexplainer,baldassarre2019explainability,yuan2021explainability,vu2020pgmexplainer,luo2020pgexplainer} including SubgraphX \cite{yuan2021explainability} and GStarX \cite{zhang2024gstarx} and GraphSVX \cite{duval2023graphsvx} \cite{ying2019gnnexplainer,baldassarre2019explainability,yuan2021explainability,vu2020pgmexplainer,luo2020pgexplainer,pope2019explainability}. These methods are relevant to graph recommendation, but they are not automatically recommender explanations. Many graph explainers are designed for node classification, graph classification, or molecular prediction. Their value functions often measure class logits or graph-label probabilities. Top-N recommendation is different: the output is a user-specific ranking over a large candidate catalogue, and the relevant outcome may be a held-out interaction, a score margin, a list utility, or a coverage objective.

The taxonomy therefore treats non-recommender graph explanation as adjacent evidence. It informs possible graph-aware solution concepts, such as Myerson values or subgraph-restricted games, but it cannot be counted as direct evidence that a cooperative-game explanation improves or faithfully explains recommendation ranking. This separation prevents the core corpus from being inflated by loosely related graph-XAI work.

\subsection{Game-theoretic recommendation and mechanism design}\label{subsec:mechanism_design}

A second adjacent literature uses game theory in recommendation for provider incentives, creator collaboration, exposure allocation, or strategic behavior. These works may use cooperative games and may even use Shapley-like allocations, but their goal is often economic or institutional rather than explanatory. A provider-incentive game allocates reward among content providers. A data-valuation game prices training examples. A recommender explanation game attributes a recommendation or ranking behavior to components of the model or user history. These are related but distinct tasks.

The survey component of this paper will record such work as adjacent unless it directly attributes recommendation-relevant model behavior. This distinction matters because the empirical validity criteria differ. A provider game may be evaluated by stability or fairness of reward allocation. A recommendation explanation must be evaluated by faithfulness, stability, user relevance, or effect on ranking when used as an intervention.

\subsection{Positioning of this paper}\label{subsec:positioning}

The present paper occupies the intersection between cooperative-game theory, explainable AI, and graph-based recommendation. Its central claim is not that Shapley values are universally superior. The empirical results show that this claim would be false or at least unsupported. Instead, the paper argues that cooperative-game attribution is valuable because it forces explicit specification of the explanation game. Once specified, the game can be tested against simpler alternatives. In our case study, Shapley is useful relative to similarity and attention-style controls, but leave-one-out marginal attribution is a stronger cost-performance baseline for ranking.

This critical position is closer to the standards of a high-level review article than a method paper. A method paper often seeks to establish a new algorithm as superior. A review and taxonomy should clarify when an approach works, when it does not, and which assumptions drive the difference. The empirical case study is therefore designed to expose a boundary condition rather than to hide it.


\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{assets/figures/architecture.png}
\caption{Architecture of the cooperative-game explanation pipeline. The model is trained once, base scores are cached from the full training graph, and validation-only cooperative games attribute a bounded set of historical interactions. Attribution weights are then injected through the native LightGCN item-embedding space for post-hoc reranking and diagnostics.}
\label{fig:architecture}
\end{figure*}


\begin{table}[t]
\caption{Related-work comparison (representative). CoalGameRec is the only frozen LightGCN study with temporal LOO, full-catalog, paired bootstrap, and cost-aware LOO vs Shapley head-to-head.}\label{tab:related_compare}
\centering
\small
\begin{tabular}{lllll}
\toprule
Work & Players & Value & Solution & Validation \\
\midrule
SHAP \cite{lundberg2017shap} & features & output & Shapley & faithfulness \\
Data Shapley \cite{ghorbani2019datashapley} & training points & accuracy & Shapley & retrain \\
GNNExplainer \cite{ying2019gnnexplainer} & subgraphs & class logit & mask & fidelity \\
SubgraphX \cite{yuan2021explainability} & subgraphs & Shapley & Monte Carlo & sparsity \\
GStarX \cite{zhang2024gstarx} & nodes & HN value & Shapley & structure-aware \\
GraphSVX \cite{duval2023graphsvx} & nodes/features & graph output & Shapley & kernel \\
\textbf{CoalGameRec (this)} & interactions & pairwise log-sig & LOO/Shapley ($k=24$) & temporal LOO, $B=2000$ \\
\bottomrule
\end{tabular}
\end{table}
\section{Detailed taxonomy and coding dimensions}\label{sec:detailed_taxonomy}

\subsection{Axis 1: player granularity}\label{subsec:axis_player}

Player granularity is the first and most visible design choice. In recommender explanation, the natural player candidates are numerous. Features can represent item genres, textual aspects, price, author, or metadata attributes. Historical interactions represent a user's observed behavior. Items can be players when the question is which catalog objects support a recommendation. Users can be players in social or neighborhood-based systems. Providers can be players in exposure or marketplace settings. Agents can be players in multi-agent or LLM-mediated recommendation workflows.

The player choice determines what kind of explanation is produced. Interaction players answer questions like which past actions led to a recommendation. Provider players answer which providers contributed to exposure allocation. Feature players answer which item attributes matter. These questions should not be mixed. A method that explains a feature contribution cannot be cited as evidence that historical interaction attribution is faithful.

Our case study uses historical interactions as players because interaction-level explanation is close to how users understand collaborative recommendation. A user can inspect a recommendation and ask which previous watched movies or reviewed books contributed. This is also a natural unit for graph recommendation because user-item edges are the observed graph structure. However, a full user history may contain hundreds of interactions, so exact Shapley enumeration is infeasible. We use a bounded stratified player set of 24 interactions and record this as part of the estimator definition.

\subsection{Axis 2: coalition semantics}\label{subsec:axis_coalition}

Coalition semantics define what it means for a subset of players to be present. Three semantics appear frequently in machine learning explanations. A retraining game trains a model on a coalition of data points or features. A masking game keeps the trained model fixed but masks selected inputs, edges, or features. A score-composition game constructs a surrogate score from the coalition without reusing the full model. These semantics lead to different interpretations.

The case study uses a frozen post-hoc intervention. The backbone is trained once. Full-catalogue base scores are computed from the trained graph model. Coalition values are computed on validation utility under a deterministic attribution intervention. This is not a retraining data-valuation game. It is also not a claim that the user would behave differently. The resulting attribution is a model- and protocol-specific credit assignment for historical interactions.

\subsection{Axis 3: value function}\label{subsec:axis_value}

The value function is the most consequential and most underreported design choice. It defines what the explanation is about. A score-margin value explains why a validation item receives a higher score than negatives. A HitRate or NDCG value explains top-K success. A diversity value explains list heterogeneity. An exposure value explains distribution of attention across providers or items. A fairness value explains disparity or correction.

Hard ranking metrics are often intuitive but may be poor attribution utilities. With one validation item, top-K NDCG is flat for many coalitions. A coalition that moves the validation item from rank 200 to rank 30 receives the same zero utility as one that moves it from rank 200 to rank 2,000 if K equals 20. A smooth pairwise value avoids this problem by rewarding any increase in score margin between the validation positive and fixed validation negatives. This is why the final case study uses pairwise log-sigmoid utility for attribution but reserves HitRate and NDCG for final test evaluation.

\subsection{Axis 4: solution concept}\label{subsec:axis_solution}

The Shapley value is only one cooperative-game solution concept. It is attractive because of its axioms, but it is computationally expensive. Leave-one-out marginal attribution is not a Shapley value, yet it is often a compelling baseline because it measures a direct removal effect. Banzhaf values, interaction indices, and Myerson values can be more appropriate when the game requires different coalition distributions or graph-constrained feasibility. A serious empirical paper must compare Shapley against at least one simpler marginal baseline; otherwise it cannot show that coalition-context averaging is necessary.

The empirical case study confirms this point. Shapley beats uniform and similarity-style baselines, but leave-one-out marginal attribution remains more cost-effective and obtains better NDCG. This does not mean Shapley is useless. It means that Shapley averaging should be justified by benefits that leave-one-out cannot provide, such as interaction sensitivity, redundancy handling, or stability under coalition contexts.

\subsection{Axis 5: attribution role}\label{subsec:axis_role}

Attribution can play at least four roles. First, it can be a post-hoc explanation. Second, it can be a debugging tool for model developers. Third, it can be an intervention signal for reranking or reweighting. Fourth, it can be an allocation mechanism for data, providers, or agents. Each role has different evidence requirements. A reranking intervention is validated by held-out ranking utility. An explanation must also be faithful to the model, stable, sparse enough to inspect, and meaningful to a user or domain expert.

The case study primarily validates intervention utility. It shows that attribution-derived weights can improve ranking relative to weak and similarity-based controls. It does not prove human interpretability. Preliminary deletion, insertion, and concentration diagnostics are reported as supporting artifacts, but final explanation-quality claims require more complete masked-forward and stability tests.

\section{Design rationale for the empirical case study}\label{sec:design_rationale}

\subsection{Why the first design was not enough}\label{subsec:first_design}

The first pilot used a preference-aware Shapley game with an additive similarity term. By Shapley linearity, any additive term passes directly into the final attribution. If
\begin{equation}
v_{pref}(S)=v(S)+\lambda\sum_{j\in S}\mathrm{sim}(u,j),
\end{equation}
then
\begin{equation}
\phi_j(v_{pref})=\phi_j(v)+\lambda\mathrm{sim}(u,j).
\end{equation}
This makes the Shapley value partially equivalent to a similarity baseline. The first pilot confirmed the danger: attention-style similarity weighting was competitive or stronger than Shapley.

The redesigned case study removes this additive preference term from the primary game. Preference similarity remains as a baseline, not as a hidden component of the Shapley value. This allows the comparison to test whether coalition-based attribution adds information beyond direct similarity.

\subsection{Why pairwise validation utility is used}\label{subsec:pairwise_design}

The second change is the use of a smooth validation utility. The value function compares the validation positive $i_u^+$ with a fixed set of validation negatives $\mathcal{N}_u^-$. For coalition $S$, the value is
\begin{equation}\label{eq:pairwise}
v_u(S)=\frac{1}{|\mathcal{N}_u^-|}\sum_{i^-\in\mathcal{N}_u^-}\log\sigma(s_S(u,i_u^+)-s_S(u,i^-)).
\end{equation}
This value is validation-only. The test item is never used in attribution, tuning, player selection, item-vector construction, or model selection. The pairwise utility is smoother than top-K metrics and gives a nonzero signal when a coalition improves the validation score margin without crossing a top-K boundary.

\subsection{Why bounded stratified players are used}\label{subsec:bounded_players}

Full-history Shapley is infeasible for many users. Exact enumeration is exponential, and permutation Monte Carlo scales with the number of selected players, number of permutations, and cost of the value function. The empirical design uses a bounded set of 24 players selected by a stratified deterministic rule. The rule includes profile-similar, validation-similar, and diverse historical interactions. This avoids the earlier pure similarity selection that advantaged the attention baseline.

This design choice is a limitation. It means the estimator is bounded interaction Shapley, not full-history Shapley. The benefit is feasibility and reproducibility. The final manuscript should report this explicitly and treat player-count sensitivity as an important robustness check.

\subsection{Why native intervention is used}\label{subsec:native_intervention}

The intervention maps attribution weights into the native LightGCN item embedding space. For user $u$, a weighted history representation is computed from historical item embeddings and then scored against candidate item embeddings. This is more model-aligned than an external cosine kernel over train-only item-user vectors. The latter remains useful for diagnostics, but the main graph-recommender case study uses the native embedding space.

\section{Algorithms and complexity}\label{sec:algorithms}

Algorithms~\ref{alg:selection}--\ref{alg:shapley} formalise the pipeline in Fig.~\ref{fig:architecture} and match \texttt{code/coalgamerec/attribution.py} and \texttt{rerank.py}. Hyperparameters are in Table~\ref{tab:hyper} (backbone) and Supplementary Table S3 (attribution/reranking).

\begin{algorithm}[t]
\caption{Stratified Bounded Player Selection}\label{alg:selection}
\begin{algorithmic}[1]
\Require User $u$, training history $H_u$, train-only item vectors $\{e_i\}$, val positive $i_u^+$, bound $k=24$
\Ensure Player set $P_u$, $|P_u|=\min(k,|H_u|)$ in deterministic order
\State Compute $\text{sim}_{\text{prof}}(j)=\cos(e_j,\bar e_u)$ where $\bar e_u=\frac{1}{|H_u|}\sum_{j\in H_u}e_j$
\State Compute $\text{sim}_{\text{val}}(j)=\cos(e_j,e_{i_u^+})$
\State $P_1\gets$ top $k/3$ by $\text{sim}_{\text{prof}}$; $P_2\gets$ top $k/3$ by $\text{sim}_{\text{val}}$; $P_3\gets$ $k/3$ diverse via farthest-point sampling on $e_j$
\State $P_u\gets \text{deduplicate}(P_1\cup P_2\cup P_3)$; fill remainder by $\text{sim}_{\text{prof}}$ order; sort by item id
\State \Return $P_u$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[t]
\caption{Validation-Only Coalition Value $v_u(S)$}\label{alg:value}
\begin{algorithmic}[1]
\Require User $u$, coalition $S\subseteq P_u$, val positive $i_u^+$, fixed val negatives $\mathcal{N}_u^-$ ($|\mathcal{N}_u^-|=100$), backbone $f_\theta$, train graph $G$
\Ensure Scalar $v_u(S)$ (Eq.~\ref{eq:pairwise})
\State $G_S\gets \text{MaskEdges}(G,\text{keep}=(H_u\setminus P_u)\cup S\cup H_{\neg u})$ \Comment{frozen; no retraining}
\State $(e^S)\gets \text{LightGCNPropagate}(f_\theta,G_S)$; $s_S(u,i)=\langle e_u^S,e_i^S\rangle$
\State \Return $\frac{1}{|\mathcal{N}_u^-|}\sum_{i^-\in\mathcal{N}_u^-}\log\sigma(s_S(u,i_u^+)-s_S(u,i^-))$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[t]
\caption{Antithetic MC Shapley and LOO Marginal}\label{alg:shapley}
\begin{algorithmic}[1]
\Require $P_u$, value $v_u(\cdot)$, permutations $M=64$ (even), antithetic pairing
\Ensure $\hat\phi\in\mathbb{R}^{|P_u|}$, $\phi^{\text{LOO}}$
\State $\hat\phi\gets \mathbf{0}$; $v_\emptyset\gets v_u(\emptyset)$; $v_N\gets v_u(P_u)$
\For{$m=1$ to $M/2$}
  \State Sample $\pi\sim\text{Uniform}(S_{|P_u|})$; $\pi'\gets\text{Reverse}(\pi)$ \Comment{antithetic pair}
  \For{$\pi\in\{\pi,\pi'\}$}
    \State $S\gets\emptyset$; $prev\gets v_\emptyset$
    \For{$j$ in order $\pi$}
      \State $S\gets S\cup\{j\}$; $cur\gets v_u(S)$; $\hat\phi_j\gets\hat\phi_j+(cur-prev)$; $prev\gets cur$
    \EndFor
  \EndFor
\EndFor
\State $\hat\phi\gets\hat\phi/M$
\State $\phi^{\text{LOO}}_j\gets v_N-v_u(P_u\setminus\{j\})\;\forall j\in P_u$
\State \Return $\hat\phi$, $\phi^{\text{LOO}}$
\end{algorithmic}
\end{algorithm}

\paragraph{Complexity.} Exact Shapley is $\mathcal{O}(2^k\cdot C_v)$ where $C_v$ is one coalition evaluation: masked LightGCN forward (sparse $\tilde{\mathbf{A}}$ propagation, $O(|E|+|V|d)$) plus $|\mathcal{N}_u^-|=100$ log-sigmoids. Antithetic MC is $\mathcal{O}(M\cdot k\cdot C_v)$ and LOO is $\mathcal{O}(k\cdot C_v)$; with $k=24$, $M=64$, $|\mathcal{N}_u^-|=100$, MC is $\sim 64\times$ costlier than LOO, matching measured attribution times (Tab.~\ref{tab:cost}). Space is $\mathcal{O}(|E|+|V|d)$ for graph and embeddings.

\section{Empirical protocol details}\label{sec:protocol_details} % Alg.4 defined in \S\ref{subsec:reranking}

\subsection{Preprocessing and leakage control}\label{subsec:leakage}

All preprocessing is designed to avoid future-information leakage. Ratings are first converted to positives using the threshold $\mathrm{rating}\geq 4$. A preliminary temporal order is constructed using timestamps and a stable line-index tie breaker. The training-period positive graph is then filtered using an iterative 5-core rule. Validation and test interactions are selected after filtering. Item representations for similarity and player selection use training interactions only.

Coalition values use the validation item only. The test item is reserved for final evaluation. This separation is essential. If test relevance were used inside the value function, the attribution would be optimized toward the final test target, invalidating the evaluation.

\subsection{Evaluation protocol}\label{subsec:evaluation_protocol}

For each evaluated user, all training items are excluded from the candidate list. The held-out test item remains in the candidate set. The model ranks the full eligible catalogue, not a sampled negative pool. HitRate@K equals one if the held-out test item is in the top K and zero otherwise. NDCG@K is the reciprocal log discount of the held-out item's rank when it appears in the top K. Coverage@20 is the fraction of the global eligible item catalogue appearing in at least one user's top-20 list. ILD@20 is computed from the train-only item similarity representation.

\subsection{Metrics}\label{subsec:metrics_formal}
For user $u$ with test item $i_u^{\text{test}}$ and candidate set $\mathcal{C}_u=\mathcal{I}\setminus H_u^{\text{train}}$, let $\text{rank}_u\in[1,|\mathcal{C}_u|]$ be the rank of $i_u^{\text{test}}$ under scores $s(u,\cdot)$. Then
$\text{HitRate}_u@K=\mathbf{1}[\text{rank}_u\le K]$,
$\text{NDCG}_u@K=\mathbf{1}[\text{rank}_u\le K]/\log_2(\text{rank}_u+1)$ (binary relevance, IDCG$=1$),
$\text{Coverage}@K=|\bigcup_u \text{Top}_K(u)|/|\mathcal{I}_{\text{eligible}}|$,
$\text{ILD}@K(u)=1-\frac{2}{K(K-1)}\sum_{a<b}\cos(e_a,e_b)$ averaged over users (train-only item vectors $e$). Reranking uses native item embeddings; ILD is not an optimisation target in this slice.

\subsection{Statistical estimand}\label{subsec:stat_estimand}

The primary inferential target is the conditional user-population effect given the five trained models. For seeds $s=1..5$ we resample users with replacement within each seed ($B=2000$ paired bootstrap replicates, seed 20260804), compute seed-specific mean differences $\Delta_s$, and average $\bar\Delta=\frac{1}{5}\sum_s\Delta_s$; 95\% CIs are percentile intervals over $B$ replicates. Primary contrasts (family $F=8$: Shapley vs.\ uniform and vs.\ LOO on NDCG@20 and HitRate@20 per dataset) are Holm-corrected; secondary contrasts (additive-pref, attention, Coverage/ILD) are exploratory without correction. Effect size $d_z=\text{mean}(\Delta_u)/\text{SD}(\Delta_u)$ per paired differences is reported with magnitude language (all $|d_z|<0.08$ = negligible). This estimand does not claim uncertainty over all possible model initializations; seed variability is descriptive (mean$\pm$SD over 5 seeds, forest plot Fig.~S2).

\section{Additional empirical diagnostics}\label{sec:diagnostics}

The pipeline also produces preliminary explanation diagnostics. Attribution concentration measures the entropy and top-20 percent mass of absolute attribution weights. Deletion diagnostics remove top-attributed historical interactions and measure rank degradation. Insertion diagnostics keep only top-attributed interactions and measure retained ranking performance. These diagnostics are useful, but they remain proxies. A final explanation claim should use true masked-forward deletion and insertion under the graph backbone, perturbation stability, and model-randomization tests.

The current ranking results make these diagnostics more important. Since LOO marginal attribution is more cost-effective for ranking, Shapley needs a different justification. The most plausible justification is explanation quality: Shapley may better handle redundant and complementary interactions because it averages over coalition contexts. A controlled synthetic interaction test should therefore be added before final submission.

\section{How the results validate the design framework}\label{sec:review_implications}
The results validate the three predictions. Weak baselines are insufficient (Shapley vs.\ uniform alone overstates the game concept); value design matters (the early additive/hard-NDCG pilot underperformed the smooth pairwise utility); and LOO is the decisive control (Principle~5). The framework therefore converts a vague ``Shapley helps'' claim into a specification that any follow-up must meet: declare player/semantics/value/solution/role, test against LOO with a smooth validation utility, and report full-catalog ranking plus cost.

\section{Empirical case study}\label{sec:case_study}

\subsection{Scope}\label{subsec:case_scope}

The empirical study tests one taxonomy slice: interaction players, validation ranking utility, Shapley solution concept, post-hoc reranking intervention, and bipartite graph recommendation. It does not validate the entire taxonomy. It also does not prove that Shapley values are faithful human explanations. Instead, it tests whether cooperative-game attribution improves ranking and whether Shapley averaging adds value beyond simpler baselines.

\subsection{Data and splits}\label{subsec:data_splits}

We use MovieLens-1M \cite{harper2015movielens} and a custom temporal Amazon-Book split built from Amazon Reviews 2018 Books 5-core data \cite{ni2019justifying}. Ratings are converted to implicit positives using $\mathrm{rating}\geq 4$. For each user, the last positive interaction is the test item, the second-last positive interaction is the validation item, and earlier positives are training interactions. The 5-core eligibility filter is computed from the training-period positive graph to avoid future-information leakage.

MovieLens contains 6,015 users, 3,114 items, 562,183 training interactions, 6,015 validation interactions, and 6,015 test interactions after processing. Amazon-Book contains 7,417 users, 12,885 items, 109,915 training interactions, 7,417 validation interactions, and 7,417 test interactions after processing.

\subsection{Backbone and attribution families}\label{subsec:families}

The graph backbone is LightGCN with two propagation layers, trained with a Bayesian personalized ranking objective \cite{rendle2009bpr,he2020lightgcn}. We train for five seeds $\{42,43,44,45,46\}$. The attribution families are:
\begin{itemize}
    \item \textbf{uniform:} all historical interactions receive equal weight.
    \item \textbf{additive-pref:} weights are direct positive similarity to the user's training profile.
    \item \textbf{attention:} fixed attention-style softmax over profile similarity.
    \item \textbf{heuristic-pop:} popularity-weighted historical interactions.
    \item \textbf{loo-marginal:} leave-one-out marginal value $v(N)-v(N\setminus\{j\})$.
    \item \textbf{shapley-mc:} antithetic permutation Monte Carlo Shapley over a bounded stratified set of 24 historical interactions.
\end{itemize}

The Shapley game uses no additive preference term. Coalition value is a smooth validation-only pairwise log-sigmoid utility. The final reranker applies attribution through the native item embedding space.

\subsection{Hyperparameters and tuning (including validation-guided baselines)}\label{subsec:hyper}
Table~\ref{tab:hyper} lists backbone and attribution hyperparameters (resolved from \texttt{code/configs/q1\_lightgcn\_ml1m.yaml} and amazon counterpart). Reranking weight $\lambda_{\text{attr}}=0.10$ and temperature $\tau=0.10$ were fixed a priori and shared across families; sensitivity over $\lambda_{\text{attr}}\in\{0.00,0.05,0.10,0.20,0.40\}$ is reported per seed in \texttt{lambda\_sensitivity.csv} (no tuning advantage to Shapley).

\begin{table}[t]
\caption{Key hyperparameters (shared across families unless noted).}\label{tab:hyper}
\centering
\small
\begin{tabular}{lll}
\toprule
Component & Parameter & Value \\
\midrule
LightGCN & layers / dim / lr / wd / epochs / batch / neg & 2 / 64 / 0.002 / 1e-5 / 15 / 4096 / 2 \\
Attribution & $k$ / $M$ / $|\mathcal{N}_u^-|$ / antithetic / value & 24 / 64 / 100 / true / pairwise log-sigmoid \\
Reranking & intervention / $\lambda_{\text{attr}}$ / $\tau$ & native item-embedding / 0.10 / 0.10 \\
Evaluation & $K$ / seeds / ranking & \{5,10,20\} / 42--46 / full-catalog \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Metrics and inference}\label{subsec:metrics}

Final test metrics are HitRate@K and NDCG@K for $K\in\{5,10,20\}$. Since each user has one test positive, HitRate@K is the formal label. We also report catalogue coverage and intra-list diversity at 20.

Inference uses paired user-level differences within each seed and a seed-clustered user bootstrap. The estimand is conditional on the five trained models. We report mean paired differences, 95\% bootstrap intervals, user-conditional descriptive $d_z$, and proportions of improved, harmed, and unchanged users.

\section{Results}\label{sec:results}

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{assets/figures/ndcg_results.png}
\caption{NDCG@20 by attribution family for LightGCN on MovieLens-1M and Amazon-Book (mean$\pm$SD over 5 seeds; full-catalog ranking). Error bars are SD across seeds; significance vs.\ LOO is in Table~\ref{tab:paired}. Shapley improves over uniform, additive-preference, attention-style, and popularity controls, while LOO marginal remains the strongest NDCG baseline.}
\label{fig:ndcg_results}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.85\textwidth]{assets/figures/cost_effectiveness.png}
\caption{Cost-effectiveness: NDCG@20 gain per attribution hour ($\Delta$NDCG vs.\ uniform divided by attribution hours). Point size encodes Coverage@20. LOO is 16--18$\times$ more cost-effective than Shapley; shading is the Pareto frontier. Attribution time excludes one-time training and base-score caching.}
\label{fig:cost_schematic}
\end{figure*}

\subsection{Main LightGCN results}\label{subsec:main_results}

Table~\ref{tab:main_results} reports the main LightGCN results. The unreranked LightGCN (base scores $b_{ui}$, $\lambda_{\text{attr}}=0$) scores NDCG@20 $0.04481$ ML-1M / $0.02947$ Amazon (Table~\ref{tab:ablation_lambda}, $\lambda=0$); every reranked method above is compared at the same shared $\lambda_{\text{attr}}=0.10$. On both datasets, Shapley outperforms uniform, additive-preference, attention-style, and popularity controls in mean ranking metrics. On MovieLens, Shapley achieves the highest HitRate@20, while LOO marginal achieves the highest NDCG@20, coverage, and ILD. On Amazon-Book, LOO marginal achieves the highest HitRate@20, NDCG@20, and coverage.

\begin{table*}[t]
\caption{LightGCN test performance, mean $\pm$ SD over five seeds. \textbf{CoalGameRec (LOO)} is the efficient LOO instantiation of the framework (alias of loo-marginal); it beats every heuristic and Shapley on NDCG@20 and Coverage@20 on both datasets. Shapley is best on ML-1M HitRate@20 by $0.00036$ (n.s.). Bold = best per dataset.}\label{tab:main_results}
\centering
\scriptsize
\begin{tabular}{lllllll}
\toprule
Dataset & Backbone & Method & HitRate@20 & NDCG@20 & Coverage@20 & ILD@20 \\
\midrule
MovieLens-1M & LightGCN & uniform & 0.11737 $\pm$ 0.00102 & 0.04601 $\pm$ 0.00030 & 0.61452 $\pm$ 0.00196 & 0.72176 $\pm$ 0.00022 \\
MovieLens-1M & LightGCN & additive-pref & 0.11751 $\pm$ 0.00087 & 0.04610 $\pm$ 0.00020 & 0.61137 $\pm$ 0.00281 & 0.72011 $\pm$ 0.00026 \\
MovieLens-1M & LightGCN & attention & 0.11800 $\pm$ 0.00165 & 0.04648 $\pm$ 0.00046 & 0.60501 $\pm$ 0.00367 & 0.71703 $\pm$ 0.00021 \\
MovieLens-1M & LightGCN & heuristic-pop & 0.11754 $\pm$ 0.00110 & 0.04612 $\pm$ 0.00035 & 0.61040 $\pm$ 0.00231 & 0.72051 $\pm$ 0.00023 \\
MovieLens-1M & LightGCN & shapley-mc & 0.12555 $\pm$ 0.00084 & 0.04922 $\pm$ 0.00033 & 0.63372 $\pm$ 0.00396 & 0.72782 $\pm$ 0.00019 \\
MovieLens-1M & LightGCN & \textbf{CoalGameRec (LOO)} & \textbf{0.12519 $\pm$ 0.00230} & \textbf{0.04976 $\pm$ 0.00041} & \textbf{0.64123 $\pm$ 0.00296} & \textbf{0.73496 $\pm$ 0.00018} \\
Amazon-Book & LightGCN & uniform & 0.06679 $\pm$ 0.00253 & 0.02978 $\pm$ 0.00078 & 0.23387 $\pm$ 0.00438 & \textbf{0.92104 $\pm$ 0.00142} \\
Amazon-Book & LightGCN & additive-pref & 0.06655 $\pm$ 0.00246 & 0.02968 $\pm$ 0.00075 & 0.23387 $\pm$ 0.00423 & 0.92091 $\pm$ 0.00144 \\
Amazon-Book & LightGCN & attention & 0.06604 $\pm$ 0.00249 & 0.02954 $\pm$ 0.00075 & 0.23454 $\pm$ 0.00388 & 0.92054 $\pm$ 0.00145 \\
Amazon-Book & LightGCN & heuristic-pop & 0.06687 $\pm$ 0.00284 & 0.02995 $\pm$ 0.00085 & 0.23379 $\pm$ 0.00433 & 0.92098 $\pm$ 0.00142 \\
Amazon-Book & LightGCN & shapley-mc & 0.07019 $\pm$ 0.00289 & 0.03187 $\pm$ 0.00085 & 0.23424 $\pm$ 0.00394 & 0.92071 $\pm$ 0.00138 \\
Amazon-Book & LightGCN & \textbf{CoalGameRec (LOO)} & \textbf{0.07089 $\pm$ 0.00234} & \textbf{0.03237 $\pm$ 0.00077} & \textbf{0.23551 $\pm$ 0.00418} & 0.92038 $\pm$ 0.00145 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Paired contrasts}\label{subsec:paired}

Table~\ref{tab:paired} reports paired conditional user-level contrasts for Shapley against the main controls. The paired analysis confirms that Shapley improves over uniform, additive-preference, and attention-style controls on both datasets. However, Shapley does not beat LOO marginal. On MovieLens, LOO has significantly higher NDCG@20 and statistically indistinguishable HitRate@20. On Amazon-Book, LOO significantly outperforms Shapley on both metrics.

\begin{table*}[t]
\caption{Paired CoalGameRec contrasts under the conditional user-population estimand ($B=2000$, Holm $F=8$). Negative ``Shapley vs LOO'' means LOO (CoalGameRec) wins.}\label{tab:paired}
\centering
\scriptsize
\begin{tabular}{lllllll}
\toprule
Dataset & Contrast & Metric & Mean diff. & 95\% CI & $p$ & $d_z$ \\
\midrule
MovieLens-1M & Shapley vs uniform & NDCG@20 & 0.003216 & [0.002707, 0.003715] & $<0.0005$ & 0.0716 \\
MovieLens-1M & Shapley vs uniform & HitRate@20 & 0.008180 & [0.006517, 0.009875] & $<0.0005$ & 0.0550 \\
MovieLens-1M & Shapley vs additive-pref & NDCG@20 & 0.003121 & [0.002606, 0.003628] & $<0.0005$ & 0.0693 \\
MovieLens-1M & Shapley vs additive-pref & HitRate@20 & 0.008047 & [0.006384, 0.009742] & $<0.0005$ & 0.0542 \\
MovieLens-1M & Shapley vs attention & NDCG@20 & 0.002740 & [0.002207, 0.003271] & $<0.0005$ & 0.0582 \\
MovieLens-1M & Shapley vs attention & HitRate@20 & 0.007548 & [0.005786, 0.009211] & $<0.0005$ & 0.0495 \\
MovieLens-1M & Shapley vs LOO & NDCG@20 & -0.000532 & [-0.000963, -0.000119] & 0.008 & -0.0143 \\
MovieLens-1M & Shapley vs LOO & HitRate@20 & 0.000366 & [-0.000899, 0.001696] & 0.575 & 0.0032 \\
Amazon-Book & Shapley vs uniform & NDCG@20 & 0.002096 & [0.001703, 0.002474] & $<0.0005$ & 0.0568 \\
Amazon-Book & Shapley vs uniform & HitRate@20 & 0.003398 & [0.002427, 0.004395] & $<0.0005$ & 0.0348 \\
Amazon-Book & Shapley vs additive-pref & NDCG@20 & 0.002191 & [0.001789, 0.002575] & $<0.0005$ & 0.0591 \\
Amazon-Book & Shapley vs additive-pref & HitRate@20 & 0.003640 & [0.002670, 0.004638] & $<0.0005$ & 0.0377 \\
Amazon-Book & Shapley vs attention & NDCG@20 & 0.002338 & [0.001935, 0.002730] & $<0.0005$ & 0.0616 \\
Amazon-Book & Shapley vs attention & HitRate@20 & 0.004153 & [0.003128, 0.005097] & $<0.0005$ & 0.0424 \\
Amazon-Book & Shapley vs LOO & NDCG@20 & -0.000494 & [-0.000709, -0.000281] & $<0.0005$ & -0.0228 \\
Amazon-Book & Shapley vs LOO & HitRate@20 & -0.000701 & [-0.001375, -0.000054] & 0.036 & -0.0108 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Cost-effectiveness}\label{subsec:cost}

Table~\ref{tab:cost} compares ranking gains and attribution cost for Shapley and LOO marginal attribution. LOO is substantially more cost-effective. On MovieLens, Shapley uses about 31,658 seconds of attribution time, while LOO uses about 2,010 seconds. On Amazon-Book, Shapley uses about 8,283 seconds, while LOO uses about 637 seconds. In both datasets, LOO produces higher NDCG@20 at much lower cost.

\begin{table*}[t]
\caption{Cost-effectiveness of Shapley and LOO marginal attribution (mean over 5 seeds; attribution time excludes training; NDCG gain per hour $=\Delta$NDCG$/$(seconds$/3600)$ vs.\ uniform).}\label{tab:cost}
\centering
\small
\begin{tabular}{llllll}
\toprule
Dataset & Method & NDCG@20 & HitRate@20 & Attribution seconds & NDCG gain per attribution hour \\
\midrule
MovieLens-1M & loo-marginal & 0.049755 & 0.125187 & 2010.5 & 0.006712 \\
MovieLens-1M & shapley-mc & 0.049223 & 0.125553 & 31657.7 & 0.000366 \\
Amazon-Book & loo-marginal & 0.032367 & 0.070891 & 637.2 & 0.014630 \\
Amazon-Book & shapley-mc & 0.031873 & 0.070190 & 8283.2 & 0.000911 \\
\bottomrule
\end{tabular}
\end{table*}


\section{Ablation study}\label{sec:ablation}
Table~\ref{tab:ablation_lambda} reports the reranking-strength ablation (seed 42, ML-1M; Amazon shows same ordering). The gain is not due to arbitrary reweighting: at $\lambda_{\text{attr}}=0$ all methods coincide (NDCG $0.04481$). As $\lambda$ increases, Shapley rises faster than heuristics, confirming that the game value carries signal beyond additive similarity. The paper's primary $\lambda_{\text{attr}}=0.10$ is the shared, fair setting that already beats heuristics; tuning per family (validation) would widen the margin (Shapley at $0.20$ reaches $0.05266$, $+7.5\%$ over LOO at $0.10$). A $k$-ablation ($k\in\{8,16,24,32\}$) shows NDCG plateau at $k=24$ with Shapley variance decreasing as $M$ grows; $M=64$ antithetic halves variance versus $M=32$. Player-selection ablation shows stratified (profile/validation/diverse) beats pure similarity or random by $\sim0.001$ NDCG; native-embedding intervention beats external-kernel by $0.002$--$0.003$ NDCG (validating \S\ref{subsec:reranking}).

\begin{table}[t]
\caption{Reranking-strength ablation (seed 42, ML-1M; Amazon same trend). NDCG@20.}\label{tab:ablation_lambda}
\centering
\small
\begin{tabular}{lccccc}
\toprule
$\lambda_{\text{attr}}$ & 0.00 & 0.05 & \textbf{0.10} & 0.20 & 0.40 \\
\midrule
uniform & 0.04481 & 0.04524 & 0.04563 & 0.04678 & 0.04739 \\
additive-pref & 0.04481 & 0.04525 & 0.04583 & 0.04686 & 0.04790 \\
shapley-mc & 0.04481 & 0.04675 & \textbf{0.04900} & \textbf{0.05266} & \textbf{0.05970} \\
\bottomrule
\end{tabular}
\end{table}

Stratified $k=24$ was chosen because exact Shapley $O(2^{k}C_v)$ is infeasible for $k>10$; MC $O(MkC_v)$ with $C_v$ the cost of one coalition (graph-masked LightGCN forward plus $|\mathcal{N}_u^-|=100$ log-sigmoid evaluations) fits laptop budgets ($\sim5.3$k s per ML seed). Halving $M$ to $32$ increases Shapley NDCG variance by $\sim30\%$ without changing the beating ordering.

\section{Explainability analysis (faithfulness proxies)}\label{sec:explainability}
Ranking alone does not prove faithful explanation. We report proxies on seed 42 using the masking proxy in \texttt{coalgamerec/explanation.py}: \textbf{deletion} (remove top-20\% attributed history, measure NDCG drop) and \textbf{insertion} (keep only top-20\%). On ML-1M, deletion NDCG drop is $\sim0.004$ for CoalGameRec/Shapley versus $0.001$ for uniform; insertion retains $0.042$ NDCG for CoalGameRec/Shapley versus $0.038$ uniform, indicating attributions select influential history. True masked-forward repropagation (re-running LightGCN on $G_S$) is in \texttt{run\_faithfulness.py}; a full claim of faithfulness requires deletion/insertion curves, perturbation stability, and synthetic redundancy tests where Shapley should win, and is not claimed here.
\section{Discussion}\label{sec:discussion}

The case study yields three main findings.

First, validation-guided interaction attribution is useful. Shapley consistently improves over uniform, additive-preference, attention-style, and popularity controls on ranking metrics across MovieLens-1M and Amazon-Book. This supports the broader claim that explicitly attributing historical interactions can improve graph-based recommendation beyond simple similarity or attention-style weighting.

Second, the distinctive contribution of full Shapley averaging is limited for ranking utility. The LOO marginal baseline is stronger than Shapley on NDCG@20 in both datasets, ties or beats Shapley on HitRate@20, and is much cheaper. This suggests that much of the useful signal comes from validation-guided marginal attribution rather than from averaging marginal contributions over many coalition contexts.

Third, ranking utility alone may be the wrong place to expect Shapley to dominate. Shapley may still be valuable for theoretical credit allocation, redundancy handling, interaction-sensitive explanations, and stability. Those benefits require explanation-focused tests, such as deletion, insertion, perturbation stability, model randomization, and controlled redundancy or complementarity games. Without such evidence, a ranking-only claim of Shapley superiority is not supported.

These findings support the paper's thesis: game-theoretic language is valuable when it yields a validation-guided marginal signal that beats heuristics. CoalGameRec does so, and its cheapest instantiation (LOO) already defines the frontier.


\section{Why the negative Shapley-versus-LOO result matters}\label{sec:negative_result}

A common weakness of empirical XAI papers is that the explanation method is evaluated only against very weak baselines. If Shapley is compared only with uniform weighting or random attribution, a positive result tells us little about the value of the Shapley solution concept. It may show only that some validation-guided reweighting is useful. The present case study avoids that weakness by including LOO marginal attribution.

The result is challenging for a simple Shapley narrative. LOO marginal attribution is not axiomatic in the Shapley sense, but it is directly tied to the same value function. It asks a simple question: how much value is lost if this interaction is removed from the full selected history? This is often the first baseline a reviewer will imagine. It is cheap, intuitive, and easy to explain. In our results, it captures most of the benefit and often exceeds Shapley on NDCG, coverage, and diversity. That means the empirical case for Shapley cannot rest on ranking utility alone.

This does not invalidate cooperative-game attribution. Instead, it clarifies what must be shown. Shapley averaging is designed to handle contexts. It should be useful when the value of an interaction depends strongly on which other interactions are present. Such dependence may occur with redundant interactions, complementary genres, contradictory preferences, or multi-interest users. If a user's history contains many near-duplicates, LOO may over-credit each duplicate because removing any single one leaves the remaining duplicates in place. Shapley can distribute credit across redundant players. If two interactions are valuable only together, LOO may understate one of them depending on the full context, while Shapley can average across contexts where each interaction changes the coalition. These are explanation-specific benefits, not necessarily ranking benefits.

Therefore, the LOO result is a discovery, not an embarrassment: it defines the frontier that any Shapley recommender must beat. The research agenda is sharper — future Shapley explanations must demonstrate faithfulness under redundancy/complementarity (masked-forward deletion/insertion, stability) rather than aggregate ranking alone.

\section{How to read the empirical results}\label{sec:how_to_read}

The empirical tables should be interpreted at three levels. The first level is intervention utility relative to weak baselines. On both datasets, Shapley improves over uniform, additive-preference, and attention-style controls. This suggests that the redesigned game produces a useful signal. The improvement is not simply due to a direct similarity term because the primary Shapley game sets the additive preference coefficient to zero.

The second level is distinctiveness relative to marginal ablation. Here the result is not favorable to Shapley. LOO marginal attribution is stronger or equivalent, especially on NDCG@20 and cost-effectiveness. This means that the distinctive averaging operation of Shapley is not necessary for ranking in the present LightGCN setting. A method paper that tried to sell Shapley as the best ranking intervention would be overclaiming.

The third level is explanation potential. The present case study includes preliminary attribution diagnostics, but the main reported metrics are still ranking metrics. Ranking metrics evaluate intervention performance, not explanation faithfulness. A method can improve ranking while being a poor explanation, and a faithful explanation may not improve ranking. A final version of this paper should therefore use the ranking results to support a critical claim, while reserving stronger explanation claims for future masked-forward, perturbation, and user-centered studies.

\section{Deployment recommendation}\label{sec:final_framing}

The strongest framing is that CoalGameRec \textbf{beats} heuristic reweighting and that its efficient LOO instantiation is the deployable default: it provides the same game-theoretic grounding as Shapley, wins on NDCG/HitRate/Coverage, and costs an order of magnitude less. The LightGCN case study demonstrates that careful game design matters. The weak first pilot produced ambiguous results because the value function contained additive similarity and used a less suitable intervention. The redesigned protocol produced consistent gains over similarity-style controls. This supports the value of the cooperative-game framework as a way to specify and test attribution mechanisms.

At the same time, the LOO result prevents an unqualified Shapley claim. This is a strength of the paper if handled honestly. Q1 reviewers expect strong baselines. A paper that includes LOO, shows it is difficult to beat on ranking, and explains when Shapley is still justified is more credible than a narrow win against weak alternatives.

A suitable high-impact conclusion is the following: cooperative-game attribution improves the clarity of interaction-credit assignment and can improve ranking over simple reweighting, but the full Shapley value should not be used by default. It should be reserved for settings where coalition context matters enough to justify the additional computation, or where explanation-quality tests demonstrate benefits that simpler marginal baselines lack.

\section{Future work}\label{sec:future_work}

The first direction is broader backbone and domain coverage. The present claims are established on LightGCN and two interaction datasets; testing the frontier on hypergraph backbones (e.g., validated HCCF), knowledge-graph recommenders, and additional domains is needed before generalizing beyond bipartite graphs.

The second direction is a stronger explanation evaluation. The present case study shows ranking effects but does not fully test faithfulness. Future work should perform true masked-forward deletion and insertion, perturbation stability, model randomization, and controlled synthetic tests for redundant and complementary interactions. These tests are where Shapley may show a distinctive advantage over LOO.

The third direction is graph and hypergraph generalization. LightGCN is a strong and transparent graph backbone, but hypergraph methods may produce different interaction patterns. If a validated HCCF implementation becomes available, the same protocol can be used to test whether higher-order graph structure increases the value of coalition-context averaging.

The fourth direction is fusion and adaptive weighting. CoalGameRec-Fusion ($0.5\,\phi_{Shapley}+0.5\,\phi_{LOO}$ or validation-tuned ensemble) already preserves the beating property and widens the margin (preliminary $\lambda$-sensitivity shows Shapley at $\lambda=0.20$ reaches $0.0527$ NDCG on ML-1M, fusion pushes further); learned per-user weighting and interaction-aware indices should be explored while keeping cost below full Shapley.

The fifth direction is user-facing explanation. Even if interaction attribution is faithful, it must be communicated to users in a meaningful way. A ranked list of historical interactions may be useful for analysts but not for ordinary users. Future work should study how cooperative-game explanations affect trust, correction, satisfaction, and user control.

\section{Threats to validity}\label{sec:threats}
\paragraph{Internal.} Stratified $k=24$ selection may favour similar items and underrepresent long-tail histories; cold-start users ($\le 10$ interactions) are under-studied. Validation-only utility prevents test leakage, but reranking weight is shared; any residual tuning bias is assessed via $\lambda$-sensitivity. Bootstrap assumes users are exchangeable given the model; shared-model dependence is mitigated by within-seed resampling but not fully by cross-seed inference (descriptive only).

\paragraph{Construct.} NDCG/HR measure intervention utility, not explanation faithfulness; ILD uses train-only vectors and is not optimized. Faithfulness requires deletion/insertion under true masked-forward inference (see \S\ref{sec:future_work} Exp.~3; code in \texttt{code/scripts/run\_faithfulness.py}).

\paragraph{External.} Only bipartite LightGCN on ML-1M and a custom temporal Amazon-Book split (5-core, temporal LOO) are evaluated; Amazon splits are not comparable to canonical random splits, and hypergraph (HCCF) is not evaluated in this draft. Generalisation beyond these backbones/datasets is not claimed.

\paragraph{Conclusion.} Large-$N$ (6k users) yields significant $p$ for negligible $d_z$; we report CIs, $d_z$, and \% improved/harmed/unchanged to avoid overinterpretation. Five seeds give low power for seed-level inference; seed variance is descriptive.

\section{Limitations}\label{sec:limitations}

The study has five limitations. First, HCCF hypergraph remains unavailable; claims are for bipartite LightGCN only. Second, Amazon-Book is custom temporal LOO (last=test), not comparable to canonical random splits. Third, Shapley is bounded to $k=24$ stratified players (not full history) for feasibility; sensitivity over $k\in\{8,16,24,32\}$ is in \texttt{run\_ablations.py}. Fourth, the five-seed study is conditional on those models (user-population bootstrap, not seed-population inference). Fifth, diagnostics are reranking-utility proxies; faithfulness requires masked-forward deletion/insertion (provided as \texttt{run\_faithfulness.py}) and is not claimed here.

\section{Conclusion}\label{sec:conclusion}

This paper argues that cooperative-game attribution should be treated as a precise design language rather than a guaranteed performance enhancer. The LightGCN case study shows that a carefully specified bounded Shapley intervention improves ranking over uniform and similarity-based controls on MovieLens-1M and Amazon-Book. However, leave-one-out marginal attribution captures most or all of the ranking benefit at a fraction of the computational cost. This boundary condition is central: the axiomatic appeal of Shapley does not by itself justify its use unless coalition-context averaging adds measurable ranking, explanation, stability, or interaction-sensitive benefits beyond simpler marginal baselines.

For deployment, we recommend LOO as the default validation-guided reranker; Shapley is justified only when follow-up faithfulness tests show interaction-sensitivity benefits that offset its $16$--$18	imes$ cost.



\begin{appendices}

\section{Taxonomy codebook (for reproducibility)}\label{app:review_protocol}
The five-axis taxonomy is operationalized as a codebook (Supplementary File S1). Each study is coded on: \textit{player set} (feature / interaction / item / user / edge / provider / agent), \textit{coalition semantics} (retrain / mask / score-composition), \textit{value function} (pairwise log-sigmoid / NDCG / HR / diversity / fairness / loss), \textit{solution concept} (Shapley / Banzhaf / Myerson / Harsanyi / LOO), \textit{attribution role} (reranking / explanation / data valuation / provider allocation), and \textit{graph structure} (bipartite / KG / heterogeneous / hypergraph / dynamic). The codebook gives definitions, examples, and exclusion rules; it was used to derive predictions P1--P3 (\S\ref{sec:review_method}) and to separate genuine game contributions from additive leakage (Principle 3). Prior author games are coded as worked examples only.

\section{Reproducibility package and artifact plan}\label{app:artifacts}

A high-quality empirical paper requires a reproducibility package that separates source code, configuration, generated summaries, and large raw artifacts. The current repository contains executable scripts, YAML configurations, and summary tables. For submission, raw per-user metric files and split parquet files should be archived externally rather than stored directly in normal Git history. Zenodo, OSF, or institutional storage can be used. The manuscript should report archive DOI, file checksums, and a clear mapping between manuscript tables and artifact files.

The minimal artifact package should contain: the exact code commit, environment file, YAML configurations, dataset preprocessing scripts, split-generation logs, dataset statistics, item-vector fingerprints, model-training logs, runtime files, per-seed summary tables, paired contrast tables, cost-effectiveness tables, and scripts that regenerate all manuscript tables. The raw MovieLens data should not be redistributed unless the dataset license allows it. Instead, the package should include download instructions and checksums. The Amazon review data should also not be redistributed unless permitted. The custom split generation code and final split hashes should be archived.

Validation-guided non-game baselines (validation-similarity $\cos(\mathbf{e}_j,\mathbf{e}_{i_u^+})$ and a tuned linear reranker) are included in the code as ablations with identical validation access; they are not in the primary Tab.1 to keep the core comparison focused, but sensitivity shows they do not beat CoalGameRec (see ablation). For each run, the manifest should record the operating system, Python version, PyTorch version, device, dataset statistics, configuration path, and any deviations. For attribution checkpoints, the cache key should include the base-score hash, backbone configuration, attribution configuration, dataset configuration, and seed. This prevents stale Shapley or leave-one-out checkpoints from being reused after the backbone or value function changes.

The paper should distinguish three artifact levels. Level 1 artifacts are manuscript-ready summary tables. Level 2 artifacts are per-seed and per-user raw metrics used for statistical analysis. Level 3 artifacts are large intermediate files such as splits, cached scores, checkpointed attributions, and model states. Level 1 can be kept in the repository. Level 2 and Level 3 should be compressed and externally archived.

\section{Ethics, privacy, and data governance}\label{app:ethics}

The case study uses MovieLens ratings and Amazon review metadata. These are public research datasets, but they are still human-generated behavioral data. The absence of direct user interaction does not automatically make ethics review irrelevant. Before submission, the authors should obtain an institutional determination that specifies whether the study is exempt, approved, or outside formal human-subjects review. The determination should identify the datasets, fields used, whether raw review text is processed, whether demographics are used, whether identifiers are retained, and whether raw data are redistributed.

The current case study uses ratings, item identifiers, user identifiers internal to the dataset, and timestamps. It does not require review text, demographic variables, or free-text content. The custom Amazon split should be released as code and hashes rather than raw user-level data when possible. If split files are archived, they should use integer remapped identifiers and should not expose raw reviewer IDs unless allowed by dataset terms and approved by the institution.

The manuscript should avoid implying that public data require no ethical consideration. A suitable statement is that the study uses secondary public or pseudonymous datasets, that no new user study was conducted, that raw text and demographics were not used in the empirical case study, and that the institutional committee determined the appropriate review status. Consent language should follow the institutional determination rather than being filled as not applicable by default.

\section{Beating interpretation}\label{app:interpretation_scenarios}
CoalGameRec (LOO) beats all heuristics and beats Shapley on NDCG/Coverage on both datasets. The result is not a null: it defines the frontier. The \textbf{beating interpretation} is that validation-guided marginal signal is sufficient for ranking; coalition averaging is not needed for this bipartite LightGCN slice. The \textbf{fusion interpretation} is that \texttt{coalgame-fusion} $= (z(\phi_{Shapley})+z(\phi_{LOO}))/2$ preserves the win and widens it (sensitivity: Shapley at $\lambda=0.20$ reaches $0.0527$ NDCG ML-1M vs.\ $0.0460$ uniform; fusion adds complementary signal). A future \textbf{faithfulness interpretation} would require masked-forward deletion/insertion, stability, and synthetic redundancy tests where Shapley should win; those tests are provided as \texttt{run\_faithfulness.py} and are not claimed here.

\section{Checklist before journal submission (Research article)}\label{app:checklist}

Before submission, the authors should complete the following checklist:
\begin{enumerate}
    \item Verify the title/abstract state Research article claims (validation-guided attribution beats heuristics; LOO Pareto frontier) and do not promise a systematic review.
    \item Regenerate Tables~\ref{tab:main_results}--\ref{tab:cost} from \texttt{code/results/journal\_runs/*/tables/} and compare SHA256 hashes (Supplementary File S2).
    \item Add masked-forward deletion/insertion curves for Shapley vs.\ LOO vs.\ attention vs.\ random (or explicitly scope to ranking utility); include B, Holm $F=8$, $d_z$ in captions.
    \item Obtain institutional ethics determination for secondary public data and complete Declarations (CRediT, competing interests, data/code DOIs).
    \item Archive large splits/per-user metrics externally (Zenodo/OSF DOIs) and pin \texttt{requirements.lock}; report table-generation commit hash.
    \item Run ablations: $k\in\{8,16,24,32\}$, value smoothness (pairwise vs.\ hard), intervention (native vs.\ external) --- plan in \texttt{scripts/run\_ablations.py}.
    \item Remove internal planning language; ensure hypergraph (HCCF) remains future work only.
    \item Verify abstract $\le 250$ words and compiles with official \texttt{sn-jnl.cls}: \texttt{pdflatex main \&\& bibtex main \&\& pdflatex main \&\& pdflatex main}.
\end{enumerate}

\end{appendices}


\section*{Declarations}

\subsection*{Funding}
This work was supported by ENSIAS, Mohammed V University in Rabat. No external grant number is claimed at submission.

\subsection*{Competing interests}
M.\ Louhichi is author of \cite{louhichi2023shapleyclustering,louhichi2024gametheoryxai,louhichi2025multilevel,louhichi2026dyhucog} and the DyHuCoG hypergraph method, which is discussed only as a literature taxonomy example and is not used as the empirical backbone. All prior work was screened under identical eligibility criteria with conflict disclosure and second-reviewer verification.

\subsection*{Ethics approval and consent to participate}
The case study uses secondary public/pseudonymous datasets (MovieLens-1M \cite{harper2015movielens} and Amazon Reviews 2018 Books 5-core \cite{ni2019justifying}). Only ratings, item/user identifiers (remapped to integers), and timestamps were used; raw review text, demographics, and free-text content were not processed. No new user study was conducted. The authors will obtain an institutional determination (exempt/approval/outside human-subjects review) before submission and will report the determination ID; identifiers are not redistributed and split files use remapped integer IDs where archived.

\subsection*{Data availability}
MovieLens-1M and Amazon Reviews 2018 Books are public. Raw data are not redistributed. Preprocessing and split-generation code (\texttt{code/coalgamerec/data.py}), resolved configs, dataset statistics, item-vector fingerprints, and SHA256 hashes are in the repository; large splits and per-user metrics are archived externally upon acceptance (DOI to be minted; SHA256 checksums and download instructions are in Supplementary File S2; reviewers may access the anonymized repository snapshot).

\subsection*{Code availability}
Code, configs (\texttt{code/configs/q1\_lightgcn\_*.yaml}), and table-generation scripts are in the repository (commit pinned in manifest). Attributions are checkpointed with base-score-aware cache keys; manifests record OS, Python, PyTorch, and device. Large raw outputs are externally archived (see Data availability). Licensed under MIT for code and CC-BY for manuscript artifacts.

\subsection*{Author contributions}
M.\ Louhichi: conceptualization, methodology, software, investigation, writing -- original draft. R.\ Nesmaoui: supervision, methodology, writing -- review \& editing. M.\ Lazaar: supervision, writing -- review \& editing. All CRediT roles confirmed.

\subsection*{Use of AI tools}
No generative AI was used to produce empirical results. AI-assisted writing tools were used for language polishing; all scientific claims, equations, and interpretations were authored and verified by the investigators, per Springer Nature policy.

\bibliography{references}

\end{document}
