# Final ActionShap assets — pending rerun

No corrected numerical result is claimed in this checkout yet. The schema-v1
pilot was archived because its utility labels, candidate semantics, action
space, and statistical unit did not match the final protocol.

Run `code/scripts/run_final_suite.py` after supplying both datasets. This
directory is then rebuilt by `code/scripts/make_paper_assets.py`. Only a
`final/manifests/validation_report.json` with `status: PASS` permits numerical
claims in `paper.tex`.
