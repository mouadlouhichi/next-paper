# ActionShap specification pointer

This filename is retained to avoid broken external links. The canonical and
current specification is:

- [`ActionShap_Recommendation_Spec.md`](ActionShap_Recommendation_Spec.md), revision 4.

Do not implement from an older copy of this file. The schema-v1 pilot protocol
and numerical assets were invalidated by the Q1 audit and are preserved only
under `paper/legacy_pilot/`.
